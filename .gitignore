# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Rust / Tauri build output
src-tauri/target

# Python leftovers. The detection/OCR sidecar was deleted (detection runs in
# process now, see src-tauri/src/detect/), but the untracked FLUX inpainting
# server, its venv and its multi-GB weight caches still sit under python/.
python/.venv
python/**/__pycache__
python/_*.json
python/_*.txt
python/build-sidecar/

# Downloaded ML model caches (multi-GB HF snapshots — never commit)
python/models/

# Vendored upstream clones (GPL; re-fetched from their own repos / added as submodules later)
external/

# Copyrighted sample manga + hand-made translation sample
tsuki117raw/
tsuki117.json

# Prebuilt binaries / build artifacts
*.exe

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Local packaged build artifacts (unsigned dev bundles)
dist-app/

# Git worktrees for parallel agent sessions
.claude/worktrees/

# Internal working notes stay local
docs/

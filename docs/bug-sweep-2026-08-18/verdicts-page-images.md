1: CONFIRMED — [`withPageImages`](file:///Users/caved/dev/manga-typesetter/src/lib/page-images.js#L205) does not capture or verify the active chapter `epoch`; an async task initiated in Chapter 1 settling after [`releaseAllPageImages()`](file:///Users/caved/dev/manga-typesetter/src/lib/page-images.js#L117) (`epoch++`, `pins.clear()`) runs its `finally` block against global state, decrementing Chapter 2's colliding page ID pin count and calling [`releasePageImages(id)`](file:///Users/caved/dev/manga-typesetter/src/lib/page-images.js#L128) to prematurely revoke Chapter 2's active image ([`src/lib/page-images.js:205-220`](file:///Users/caved/dev/manga-typesetter/src/lib/page-images.js#L205-L220)).

2: CONFIRMED — In single-page mode, navigating beyond the resident window evicts the departing page and nulls its `raw`/`cleaned` URLs; a late-firing image `onload` causes [`setPageDimsForSrc(src)`](file:///Users/caved/dev/manga-typesetter/src/lib/store.svelte.js#L1250) to return `null`, triggering the fallback `if (!target) setPageDims(owner ?? page(), ...)` against `page()` (the newly arrived page) and corrupting the new page's coordinate space and box scaling with stale measurements ([`src/lib/editor/Canvas.svelte:367-368`](file:///Users/caved/dev/manga-typesetter/src/lib/editor/Canvas.svelte#L367-L368), [`src/lib/store.svelte.js:1219-1245`](file:///Users/caved/dev/manga-typesetter/src/lib/store.svelte.js#L1219-L1245)).

3: CONFIRMED — [`psdSelfTest`](file:///Users/caved/dev/manga-typesetter/src/lib/psd.js#L990) invokes [`reconstructFromProject`](file:///Users/caved/dev/manga-typesetter/src/lib/psd.js#L693), which mints fresh blob URLs for `rebuilt.raw` and `rebuilt.cleaned` via [`layerToUrl`](file:///Users/caved/dev/manga-typesetter/src/lib/psd.js#L684) (`URL.createObjectURL`); unlike [`toChapterPage`](file:///Users/caved/dev/manga-typesetter/src/lib/psd.js#L850-L881), it never executes `revokeAll(rebuilt.raw, rebuilt.cleaned)`, permanently leaking uncompressed full-page rasters in renderer memory ([`src/lib/psd.js:1004-1016`](file:///Users/caved/dev/manga-typesetter/src/lib/psd.js#L1004-L1016)).

4: CONFIRMED — [`psdSelfTest(p)`](file:///Users/caved/dev/manga-typesetter/src/lib/psd.js#L990) accesses `src.raw` and `src.cleaned` without invoking [`withPageImages`](file:///Users/caved/dev/manga-typesetter/src/lib/page-images.js#L205); when invoked on a non-resident or unminted page (`p.raw === null`), [`buildPagePsd`](file:///Users/caved/dev/manga-typesetter/src/lib/psd.js#L616) produces a PSD with empty Base layers and raster parity assertions pass vacuously, while concurrent window movement can revoke URLs mid-read ([`src/lib/psd.js:990-1043`](file:///Users/caved/dev/manga-typesetter/src/lib/psd.js#L990-L1043)).

5: CONFIRMED — [`hasRawImage`](file:///Users/caved/dev/manga-typesetter/src/lib/page-images.js#L83) checks the truthiness of the `dirs` object rather than `dirs.raws`; for a chapter with cleaned images only (`dirs = { raws: null, cleaned: '...' }`), `(dirs && p?.file)` evaluates to `true` when `p.file` is defined, erroneously including non-mintable pages in [`detectAllPages`](file:///Users/caved/dev/manga-typesetter/src/lib/sidecar.js#L96) batch targets ([`src/lib/page-images.js:83`](file:///Users/caved/dev/manga-typesetter/src/lib/page-images.js#L83), [`src/lib/sidecar.js:96`](file:///Users/caved/dev/manga-typesetter/src/lib/sidecar.js#L96)).

### Priority Summary
- **High (Findings 1 & 2):** Critical data integrity and lifecycle issues causing cross-chapter image revocation and canvas coordinate/box corruption during standard navigation.
- **Medium (Findings 3 & 4):** Memory leak of full-page raster blobs and vacuous test execution when inspecting non-resident pages in PSD self-testing.
- **Low (Finding 5):** Benign logic bug leading to redundant iteration and incorrect filter results on chapters lacking a raws directory.

1: CONFIRMED — [`log_softmax`](file:///Users/caved/dev/manga-typesetter/src-tauri/src/detect/ocr.rs#L423-L433) is executed on raw logits before setting banned tokens to `f32::NEG_INFINITY` at lines 570–573; whenever a banned token carries non-zero probability mass $P > 0$, surviving candidates remain un-renormalized with their log-probabilities deflated by $\ln(1 - P)$, diverging from HuggingFace `transformers`' `NoRepeatNGramLogitsProcessor` which masks logits prior to `log_softmax` ([src-tauri/src/detect/ocr.rs:569-573](file:///Users/caved/dev/manga-typesetter/src-tauri/src/detect/ocr.rs#L569-L573))

2: CONFIRMED — The [`HALF_TO_FULL_KANA`](file:///Users/caved/dev/manga-typesetter/src-tauri/src/detect/ocr.rs#L242-L264) table omits `('\u{FF9E}', '\u{309B}')` and `('\u{FF9F}', '\u{309C}')` from `jaconv`'s `HALF_KANA_SEION`; OCR outputs containing standalone or non-katakana voiced marks (e.g. `あﾞ` / `\u{FF9E}`) bypass [`DAKUTEN_PAIRS`](file:///Users/caved/dev/manga-typesetter/src-tauri/src/detect/ocr.rs#L229-L237) and fail to widen in [`h2z`](file:///Users/caved/dev/manga-typesetter/src-tauri/src/detect/ocr.rs#L277-L295) ([src-tauri/src/detect/ocr.rs:242-264](file:///Users/caved/dev/manga-typesetter/src-tauri/src/detect/ocr.rs#L242-L264))

**Priority Summary:**
- P1 (Med): Fix Finding 1 by applying n-gram ban masking to raw logits before [`log_softmax`](file:///Users/caved/dev/manga-typesetter/src-tauri/src/detect/ocr.rs#L423-L433) to prevent cumulative score deflation in beam search.
- P2 (Low): Fix Finding 2 by adding `\u{FF9E}` and `\u{FF9F}` mappings to [`HALF_TO_FULL_KANA`](file:///Users/caved/dev/manga-typesetter/src-tauri/src/detect/ocr.rs#L242-L264) for exact `jaconv.h2z` post-processing parity.
- Both findings represent genuine functional divergences from the upstream Python implementations (`transformers` and `jaconv`).

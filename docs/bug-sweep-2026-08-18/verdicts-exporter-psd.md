1: **CONFIRMED** — Importing a translation JSON with a 0-indexed first line (`n: 0`, `id: 0`, `number: 0`, `index: 0`) causes `Number(0) || 1` to evaluate to `1`, assigning duplicate `n: 1` to both index 0 and index 1 and breaking key lookups in [`lineByN`](file:///Users/caved/dev/manga-typesetter/src/lib/store.svelte.js#L272). ([`src/lib/importer.js:36`](file:///Users/caved/dev/manga-typesetter/src/lib/importer.js#L36))

2: **CONFIRMED** — Exporting or autosaving text JSON evaluates [`lineTags(l)`](file:///Users/caved/dev/manga-typesetter/src/lib/tags.svelte.js#L280-L284) which writes materialized arrays (`tags: []` or `tags: ['sfx']`) for every line; on re-import, [`normLine`](file:///Users/caved/dev/manga-typesetter/src/lib/importer.js#L46) retains the array, causing [`carryTagsForward`](file:///Users/caved/dev/manga-typesetter/src/lib/tags.svelte.js#L364) to evaluate `Array.isArray(l.tags)` as true and skip restoring hand-applied custom tags from previous state. ([`src/lib/text-json.js:39`](file:///Users/caved/dev/manga-typesetter/src/lib/text-json.js#L39))

3: **CONFIRMED** — Importing a PSD without embedded project XMP (foreign or Photoshop-resaved) assigns box geometry directly from layer raster ink bounds (`n.left`, `n.top`, `n.right`, `n.bottom`) instead of reading frame coordinates from `n.text.transform` and `n.text.boxBounds`, shrinking placed boxes to tight glyph bounds. ([`src/lib/psd.js:785-788`](file:///Users/caved/dev/manga-typesetter/src/lib/psd.js#L785-L788))

4: **CONFIRMED** — Exporting to PSD with an empty or whitespace-only box possessing raster-only styling ([`isRasterOnly`](file:///Users/caved/dev/manga-typesetter/src/lib/psd.js#L301-L304)) causes [`renderBoxLayer`](file:///Users/caved/dev/manga-typesetter/src/lib/exporter.js#L450) to return `null`, producing an image layer with non-zero dimensions but `imageData: undefined` and omitted `text`, triggering a crash during `ag-psd` channel serialization. ([`src/lib/psd.js:336-364`](file:///Users/caved/dev/manga-typesetter/src/lib/psd.js#L336-L364))

5: **DOWNGRADED** — Not a permanent memory leak because canvases are unreferenced locally and collected during engine GC, but does cause transient GPU/bitmap memory spikes during large chapter batch exports compared to explicit canvas dimension zeroing. ([`src/lib/exporter.js:468-471`](file:///Users/caved/dev/manga-typesetter/src/lib/exporter.js#L468-L471))

6: **CONFIRMED** — In Tauri single-page export, saving a file named without a distinct page suffix (e.g., saving as `Chapter-10.png`) or with a non-numeric page ID causes `.replace(/-\d+$/, '')` to strip numbered chapter stems into `Chapter` (or retain raw page suffixes), corrupting [`app.exportName`](file:///Users/caved/dev/manga-typesetter/src/lib/exporter.js#L687) for subsequent exports. ([`src/lib/exporter.js:686`](file:///Users/caved/dev/manga-typesetter/src/lib/exporter.js#L686))

### Priority Summary
- **P1 (Immediate Fix)**: Finding 4 (export crash on empty raster-styled boxes) and Finding 1 (silent line ID corruption and box binding collision on 0-indexed translations).
- **P2 (High Priority)**: Finding 2 (tag loss across text-JSON export/re-import cycles) and Finding 3 (box frame collapse on foreign PSD text layers).
- **P3 (Medium/Maintenance)**: Finding 6 (Tauri export stem corruption on numbered project titles) and Finding 5 (batch export memory spikes before GC cleanup).

1. [`src/lib/page-images.js:205`](file:///Users/caved/dev/manga-typesetter/src/lib/page-images.js#L205-L220) — HIGH — [`withPageImages`](file:///Users/caved/dev/manga-typesetter/src/lib/page-images.js#L205) `finally` block mutates pins and revokes images belonging to subsequent chapters after an epoch change — An async operation (e.g. OCR detection or PSD export) in Chapter 1 awaits [`withPageImages(p1, fn)`](file:///Users/caved/dev/manga-typesetter/src/lib/page-images.js#L205), setting `pins.set('p1', 1)`. While `fn` is in flight, the user opens Chapter 2, which runs [`releaseAllPageImages()`](file:///Users/caved/dev/manga-typesetter/src/lib/page-images.js#L117) (`epoch++`, `pins.clear()`). Chapter 2 begins an operation on its own page with colliding ID `'p1'`, setting `pins.set('p1', 1)`. Chapter 1's `fn` finally completes and enters `finally`: finding `pins.get('p1') === 1` from Chapter 2, it decrements it to 0, deletes Chapter 2's pin, and calls [`releasePageImages('p1')`](file:///Users/caved/dev/manga-typesetter/src/lib/page-images.js#L128), revoking Chapter 2's active image mid-operation. — Suggested fix: Capture `const mine = epoch;` at function entry and return immediately in `finally` if `mine !== epoch`.

2. [`src/lib/editor/Canvas.svelte:368`](file:///Users/caved/dev/manga-typesetter/src/lib/editor/Canvas.svelte#L363-L370) — HIGH — [`onCleanedLoad`](file:///Users/caved/dev/manga-typesetter/src/lib/editor/Canvas.svelte#L363) fallback overwrites the new current page's dimensions with stale measurements from an evicted page — In single-page mode, a user navigates from Page A to Page B. [`setResidentWindow`](file:///Users/caved/dev/manga-typesetter/src/lib/page-images.js#L189) evicts Page A outside the resident window and sets `pageA.raw = null; pageA.cleaned = null;`. When the browser's `onload` event fires for Page A's image decode, [`setPageDimsForSrc(src)`](file:///Users/caved/dev/manga-typesetter/src/lib/store.svelte.js#L1250) cannot locate any page owning the revoked `src` and returns `null`. The fallback `if (!target) setPageDims(owner ?? page(), ...)` executes, writing Page A's width/height onto Page B and corrupting Page B's layout/box scaling. — Suggested fix: Return early when `target` is null instead of falling back to `owner ?? page()`.

3. [`src/lib/psd.js:1004`](file:///Users/caved/dev/manga-typesetter/src/lib/psd.js#L1004) — MED — [`psdSelfTest`](file:///Users/caved/dev/manga-typesetter/src/lib/psd.js#L990) leaks uncompressed blob URLs minted during lossless round-trip reconstruction — Running [`psdSelfTest`](file:///Users/caved/dev/manga-typesetter/src/lib/psd.js#L990) calls [`reconstructFromProject`](file:///Users/caved/dev/manga-typesetter/src/lib/psd.js#L697), which mints fresh blob URLs for `rebuilt.raw` and `rebuilt.cleaned` via [`layerToUrl`](file:///Users/caved/dev/manga-typesetter/src/lib/psd.js#L688) (`URL.createObjectURL`). Unlike [`toChapterPage`](file:///Users/caved/dev/manga-typesetter/src/lib/psd.js#L850), [`psdSelfTest`](file:///Users/caved/dev/manga-typesetter/src/lib/psd.js#L990) never invokes `revokeAll(rebuilt.raw, rebuilt.cleaned)` upon completion, leaving multiple full-page raster blobs permanently leaked in renderer memory. — Suggested fix: Wrap `rebuilt` inspection in a `try...finally` block that invokes `revokeAll(rebuilt?.raw, rebuilt?.cleaned)`.

4. [`src/lib/psd.js:990`](file:///Users/caved/dev/manga-typesetter/src/lib/psd.js#L990) — MED — [`psdSelfTest`](file:///Users/caved/dev/manga-typesetter/src/lib/psd.js#L990) reads `src.raw` and `src.cleaned` without ensuring or pinning page images — Calling [`psdSelfTest(p)`](file:///Users/caved/dev/manga-typesetter/src/lib/psd.js#L990) on a non-resident or unminted page accesses `src.raw`/`src.cleaned` as `null`, causing [`buildPagePsd`](file:///Users/caved/dev/manga-typesetter/src/lib/psd.js#L616) to build an image-less document and raster parity checks to pass vacuously; if the resident window shifts during test execution, `src.raw`/`src.cleaned` can be revoked mid-read during [`imageDataFromSrc`](file:///Users/caved/dev/manga-typesetter/src/lib/psd.js#L45). — Suggested fix: Wrap the body of [`psdSelfTest`](file:///Users/caved/dev/manga-typesetter/src/lib/psd.js#L990) in `withPageImages(src, ...)`.

5. [`src/lib/page-images.js:83`](file:///Users/caved/dev/manga-typesetter/src/lib/page-images.js#L83) — LOW — [`hasRawImage`](file:///Users/caved/dev/manga-typesetter/src/lib/page-images.js#L83) checks truthiness of `dirs` container object rather than `dirs.raws` — For a chapter configured without a raws directory (`dirs = { raws: null, cleaned: '...' }`), `dirs` is truthy. [`hasRawImage(p)`](file:///Users/caved/dev/manga-typesetter/src/lib/page-images.js#L83) evaluates `(dirs && p?.file)` to true, causing batch pipelines (such as [`detectAllPages`](file:///Users/caved/dev/manga-typesetter/src/lib/sidecar.js#L88)) to filter targets that cannot be minted. — Suggested fix: Change `dirs && p?.file` to `dirs?.raws && p?.file`.

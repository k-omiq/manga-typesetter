1. [`src-tauri/src/detect/geometry.rs:63`](file:///Users/caved/dev/manga-typesetter/src-tauri/src/detect/geometry.rs#L63-L80) — **SEVERITY(high)** — Extreme aspect ratios cause `inner_w` or `inner_h` to round to 0, producing a 0-dimension network input rectangle and division by zero in coordinate inverse mapping. — Input image with extreme dimensions like $1 \times 2000$ yields $\text{ratio} = 640 / 2000 = 0.32$, so `round_py(1.0 * 0.32) as u32` produces `inner_w = 0` and `net_w = 0`. [`preprocess`](file:///Users/caved/dev/manga-typesetter/src-tauri/src/detect/panels.rs#L101-L127) passes a $[1, 3, 640, 0]$ tensor to ONNX Runtime causing execution error, and [`Letterbox::to_source`](file:///Users/caved/dev/manga-typesetter/src-tauri/src/detect/geometry.rs#L101-L114) computes `gain = 0.0`, resulting in division by zero. — `let inner_w = (round_py(w0 * ratio) as u32).max(1);` (and same for `inner_h`).

2. [`src-tauri/src/detect/geometry.rs:162`](file:///Users/caved/dev/manga-typesetter/src-tauri/src/detect/geometry.rs#L162) — **SEVERITY(med)** — Descending sort via `b.score.total_cmp(&a.score)` places `NaN` scores at the beginning of the candidate array rather than discarding or deprioritizing them. — In IEEE-754 `total_cmp`, `+NaN` is greater than all finite floats. If an ONNX output produces an anomalous detection with `score = NaN`, `b.score.total_cmp(&a.score)` evaluates `NaN.total_cmp(&0.9)` as `Greater`, placing the `NaN` box at index 0 of `boxes` where [`nms`](file:///Users/caved/dev/manga-typesetter/src-tauri/src/detect/geometry.rs#L157-L174) keeps it and potentially suppresses overlapping valid boxes. — Filter non-finite scores before sorting: `boxes.retain(|b| b.score.is_finite());`.

3. [`src-tauri/src/detect/geometry.rs:208`](file:///Users/caved/dev/manga-typesetter/src-tauri/src/detect/geometry.rs#L208) — **SEVERITY(low)** — `if best_score <= conf` comparison evaluates to `false` when `best_score` is `NaN`, allowing `NaN` confidence scores to pass the threshold check. — When an anchor's class score is `f32::NAN`, `best_score <= conf` evaluates to `false` in Rust float semantics, so `continue` is skipped and a candidate with `score: NaN` is emitted into `out`. — `if !(best_score > conf) { continue; }`

4. [`src-tauri/src/detect/geometry.rs:108`](file:///Users/caved/dev/manga-typesetter/src-tauri/src/detect/geometry.rs#L108-L111) — **SEVERITY(low)** — `f32::clamp(0.0, max)` returns `NaN` when given a `NaN` coordinate instead of clamping to page boundaries. — If an anomalous predicted box has `b.x1 = NaN` (or `gain == 0.0`), `((NaN - pad_x) / gain).clamp(0.0, src_w as f32)` returns `NaN` in Rust. Downstream in [`detect_panels`](file:///Users/caved/dev/manga-typesetter/src-tauri/src/detect/analyze.rs#L73-L87), `round_half_even(NaN as f64) as i32` converts to `0`, generating a false $[0, 0, 0, 0]$ panel box that corrupts reading order topological sorting. — `let x1 = if x1.is_finite() { x1.clamp(0.0, src_w as f32) } else { 0.0 };` (for all 4 coordinates).

1. `src/lib/importer.js:36` — HIGH — `normLine` coerces `n: 0` to `idx + 1` due to falsy evaluation in `Number(n) || idx + 1`. — In translation files containing line number `0` at index 0 and line `1` at index 1, `Number(0) || 1` evaluates to `1`, assigning both lines `n: 1` and causing duplicate key collisions in `lineByN` and box-to-line binding. — Replace with `const num = Number(n);` and `n: Number.isFinite(num) ? num : idx + 1,`.

2. `src/lib/text-json.js:39` — HIGH — `serializePageText` unconditionally materializes `tags` on every line via `lineTags(l)` instead of keeping unset tags undefined. — Exported text JSON files write `tags: ['dialogue']` (or legacy fallback) for untagged lines; re-importing this file causes `carryTagsForward` to see `Array.isArray(l.tags)` as true on every incoming line, preventing it from carrying forward the user's hand-applied custom tags. — Replace `tags,` with `...(Array.isArray(l.tags) ? { tags: l.tags.slice() } : null),`.

3. `src/lib/psd.js:785` — HIGH — `reconstructForeign` assigns text box coordinates and dimensions to raster ink bounds (`n.left`/`n.top`/`n.right`/`n.bottom`) instead of the type layer frame. — When importing a foreign PSD or a PSD re-saved by Photoshop where XMP metadata was stripped, text boxes shrink to the tight bounding box of their glyph ink (e.g. 40×24 for short centered text) rather than reading frame geometry from `n.text.transform` and `n.text.boxBounds`, displacing the box and breaking text reflow. — Derive `x, y, w, h` from `n.text.transform` translation `[tx, ty]` and `n.text.boxBounds` (falling back to raster bounds only for `RASTER_MARK` layers).

4. `src/lib/psd.js:336` — HIGH — `textLayerFor` sets non-zero box frame bounds on raster-only layers when `rendered` is null. — For an empty or whitespace-only box with a curved/roughened/flipped style (`isRasterOnly`), `rendered` is null and `imageData` is undefined while `text` is omitted; `ag-psd` encounters a non-empty image layer missing both `imageData` and `canvas`, causing errors during channel data serialization. — Set `{ left: 0, top: 0, right: 0, bottom: 0 }` when `rendered` is null on raster-only layers.

5. `src/lib/exporter.js:470` — MEDIUM — `renderPageBlob` leaks canvas backing stores across the chapter export loop. — In whole-chapter raster exports (`exportImages`), each page allocates a full-page canvas that remains resident in browser memory until garbage collection; unlike `exportStripImages` (which zeroes `canvas.width`/`height`), batch exporting large chapters creates high memory spikes. — Await `toBlob` into a local variable, zero `canvas.width = 0; canvas.height = 0;`, and return the blob.

6. `src/lib/exporter.js:686` — MEDIUM — `saveNative` strips numbered project stems from `app.exportName` on single-page export. — If a user exports a chapter whose name ends in a number (e.g., `Chapter-10`), saving a single page file named `Chapter-10.png` causes `.replace(/-\d+$/, '')` to truncate the base name to `Chapter`, corrupting `app.exportName` for all subsequent exports. — Replace `base || app.exportName` derivation to only strip `-<pageId>` matching the exported page rather than a generic `-\d+$`.

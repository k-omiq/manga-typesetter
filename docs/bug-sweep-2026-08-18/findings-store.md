[src/lib/store.svelte.js:1851](file:///Users/caved/dev/manga-typesetter/src/lib/store.svelte.js#L1851) — HIGH — Deleting a free-typed box leaves `p.activeLineN` pointing at the deleted negative line number, breaking subsequent box placements — When a user activates or selects a free-typed box (setting `p.activeLineN` to e.g. `-1`) and calls [`deleteBox`](file:///Users/caved/dev/manga-typesetter/src/lib/store.svelte.js#L1830), the line is removed from `p.lines` but `p.activeLineN` remains `-1`; subsequent canvas clicks in Place mode fail inside [`placeActiveAt`](file:///Users/caved/dev/manga-typesetter/src/lib/store.svelte.js#L1555) because `lineByN(p, -1)` returns `undefined`, silently halting placement until manual queue selection — `if (freeLine.line) { p.lines = p.lines.filter((l) => l.n !== b.lineN); if (p.activeLineN === b.lineN) p.activeLineN = firstUnplaced(p); }`

[src/lib/store.svelte.js:1087](file:///Users/caved/dev/manga-typesetter/src/lib/store.svelte.js#L1087) — HIGH — [`endEdit`](file:///Users/caved/dev/manga-typesetter/src/lib/store.svelte.js#L1073) converts a line-backed box (`b.text === null`) into an override and records a bogus undo entry on an untouched blur — When opening an inline editor on a queue-placed box, `editBefore` is initialized from [`boxOwnText(b)`](file:///Users/caved/dev/manga-typesetter/src/lib/store.svelte.js#L409) (`null`); on blur without edits, `commitText` equals the displayed line text string (`"Hello"`), causing [`setBoxText`](file:///Users/caved/dev/manga-typesetter/src/lib/store.svelte.js#L413) to write an override `b.text = "Hello"` and [`recordEdit`](file:///Users/caved/dev/manga-typesetter/src/lib/store.svelte.js#L805) to push a history entry because `"Hello" !== null`, permanently detaching the box from queue updates — `if (b.lineN != null && b.lineN > 0 && b.text == null && commitText === boxText(b, page())) return;`

[src/lib/store.svelte.js:1353](file:///Users/caved/dev/manga-typesetter/src/lib/store.svelte.js#L1353) — HIGH — [`applyDetection`](file:///Users/caved/dev/manga-typesetter/src/lib/store.svelte.js#L1337) overwrites all existing translation text and wipes out free-typed lines — Running detection on an existing page maps `p.lines` with `en: ''` and discards any free-typed lines (`n < 0`), destroying user translations entered in Translate mode or free-typed dialogue lines created on canvas — `p.lines = [...p.lines.filter(isFreeLine), ...result.lines.map((l) => ({ n: l.n, type: l.type, jp: l.jp ?? '', en: lineByN(p, l.n)?.en ?? '' }))];`

[src/lib/store.svelte.js:1368](file:///Users/caved/dev/manga-typesetter/src/lib/store.svelte.js#L1368) — HIGH — [`applyDetection`](file:///Users/caved/dev/manga-typesetter/src/lib/store.svelte.js#L1337) unconditionally wipes all placed boxes without clearing selection or settling pending edits — Running single-page or batch detection on a page with existing typesetting sets `p.boxes = []` without calling [`settleEdits`](file:///Users/caved/dev/manga-typesetter/src/lib/store.svelte.js#L828) or clearing [`app.selectedId`](file:///Users/caved/dev/manga-typesetter/src/lib/store.svelte.js#L291) / [`app.editingId`](file:///Users/caved/dev/manga-typesetter/src/lib/store.svelte.js#L292), leaving selection dangling and irrecoverably destroying placed boxes with no undo entry — `if (app.selectedId && (!target || target === page())) deselect(); p.boxes = [];`

[src/lib/store.svelte.js:2057](file:///Users/caved/dev/manga-typesetter/src/lib/store.svelte.js#L2057) — MEDIUM — [`applyBulk`](file:///Users/caved/dev/manga-typesetter/src/lib/store.svelte.js#L2044) searches only `p.boxes` (`page().boxes`), dropping selected targets across other pages in longstrip mode — In longstrip mode where multiple slices are visible, target boxes selected across non-active pages resolve to `undefined` in `p.boxes.find((x) => x.id === id)`, silently omitting those targets from bulk styling and history recording — `app.bulk.targets.map((id) => { for (const pg of app.pages) { const b = pg.boxes.find((x) => x.id === id); if (b) return { page: pg, box: b }; } }).filter(Boolean)`

[src/lib/store.svelte.js:1870](file:///Users/caved/dev/manga-typesetter/src/lib/store.svelte.js#L1870) — MEDIUM — [`deleteBox`](file:///Users/caved/dev/manga-typesetter/src/lib/store.svelte.js#L1830) does not clear [`app.editingId`](file:///Users/caved/dev/manga-typesetter/src/lib/store.svelte.js#L292) when deleting a box currently in inline edit mode — If a box undergoing inline text edit is deleted (e.g. via keyboard shortcut or parent action), `editingId` is retained, causing subsequent keyboard routing and editor components to behave as if an active text caret is still open — `app.selectedId = null; if (app.editingId === id) app.editingId = null;`

[src/lib/store.svelte.js:776](file:///Users/caved/dev/manga-typesetter/src/lib/store.svelte.js#L776) — MEDIUM — [`markSaved`](file:///Users/caved/dev/manga-typesetter/src/lib/store.svelte.js#L776) does not cancel an active debounce timer (`saveT`) — When [`markUnsaved`](file:///Users/caved/dev/manga-typesetter/src/lib/store.svelte.js#L760) schedules `saveT` (e.g. during project loading or prior edits), a subsequent call to `markSaved` sets `app.saved = true` but leaves `saveT` running, triggering a redundant disk write via `runSavers()` 800ms later — `export function markSaved() { clearTimeout(saveT); app.saved = true; }`

[src/lib/store.svelte.js:2271](file:///Users/caved/dev/manga-typesetter/src/lib/store.svelte.js#L2271) — LOW — [`rawZoomBy`](file:///Users/caved/dev/manga-typesetter/src/lib/store.svelte.js#L2270) zooms to 0.8x scale when zooming out while in Fit mode (`rawZoom === 0`) — Calling [`rawZoomOut`](file:///Users/caved/dev/manga-typesetter/src/lib/store.svelte.js#L2277) when `rawZoom` is 0 calculates `(0 === 0 ? 1 : 0) * (1 / 1.25) = 0.8`, setting `app.rawZoom = 0.8` instead of remaining at 0 (Fit) as documented — `if (app.rawZoom === 0 && factor < 1) return; const next = (app.rawZoom === 0 ? 1 : app.rawZoom) * factor;`

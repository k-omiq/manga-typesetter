- `src/lib/editor/strip.js:55` — HIGH — `pageIndexAtCenter` returns the last page index `n - 1` instead of `0` when opening an unmeasured chapter (`h: 0`) — When opening a longstrip chapter, pages have `h: 0` before images decode, so `tops` is all identical; `center >= tops[i]` evaluates to true for all pages, jumping `app.pageIndex`, queue, and prefetch window to the chapter end on startup — In `pageIndexAtCenter`, break immediately if `i > 0 && tops[i] <= tops[i - 1]`.
- `src/lib/editor/Canvas.svelte:448` — HIGH — Scroll-driven `syncStrip` fights `focusPage`, immediately deselecting active boxes and aborting inline text editing on visible adjacent slices — Clicking or editing a text box on an adjacent visible slice focuses that page, but the very next scroll or rAF tick re-derives `pageIndex` from viewport center and calls `gotoPage()`, clearing `editingId` and `selectedId` — Skip `gotoPage` in `syncStrip` if the user is currently editing or if a box on the focused page is selected.
- `src/lib/editor/EditorRoot.svelte:106` — MEDIUM — `EditorRoot` untracks page height reads, preventing `residentRadiusFor` from updating when unmeasured page heights arrive — On initial mount of a longstrip chapter, `app.pages` heights are 0 so `residentRadiusFor` returns `max = 12`; when images decode and `p.h` updates via `setPageDims`, the untracked effect never re-runs and the prefetch window remains stuck at the initial radius until a manual page/zoom change — Read `pages.map(p => p.h)` outside `untrack` in `EditorRoot.svelte`'s effect.
- `src/lib/editor/Canvas.svelte:557` — MEDIUM — Array-indexed binding `bind:this={frameEls[i]}` retains stale detached DOM elements when `app.pages` length shrinks — When switching from a longer chapter to a shorter one, `frameEls` keeps trailing entries from the previous chapter, creating stale or mismatched element references during strip operations — Truncate or reset `frameEls.length = app.pages.length` when `app.pages` changes.
- `src/lib/TextBox.svelte:320` — MEDIUM — Rotate handle drag accesses `pageFrameEl.getBoundingClientRect()` which throws a `TypeError` if `pageFrameEl` prop is not yet populated by parent's array binding — On initial render of `<TextBox>`, `bind:this={frameEls[i]}` in `Canvas` has not executed yet so `pageFrameEl` is undefined, causing a runtime crash if rotation occurs before the prop updates — Use `(e.currentTarget.closest('.page-frame') ?? pageFrameEl).getBoundingClientRect()` in `startTransform`.
- `src/lib/store.svelte.js:1745` — MEDIUM — `addEmptyBox` unconditionally clamps coordinates against `Math.max(0, p.w - w)`, collapsing newly created boxes to `(0, 0)` on unmeasured pages — When creating an empty box on a page whose art has not finished decoding (`p.w === 0, p.h === 0`), `Math.max(0, p.w - w)` evaluates to 0, placing the box at `(0, 0)` in the top-left corner — Guard clamping with `p.w > 0 ? clamp(imgX - w / 2, 0, Math.max(0, p.w - w)) : Math.max(0, imgX - w / 2)` for both axes.
- `src/lib/editor/RefSidebar.svelte:41` — LOW — `RefSidebar` scroll sync effect does not recompute `scrollTop` when reference slot aspect ratios update from image measurements — When `pg.w` and `pg.h` are populated by Canvas image decodes, `aspect-ratio: ratioOf(pg)` updates sidebar slot heights and changes `scrollHeight`, but `scrollTop` is not adjusted until the user scrolls Canvas — Observe sidebar container resize or depend on `app.pages.map(p => p.w + 'x' + p.h)` in the sync effect.

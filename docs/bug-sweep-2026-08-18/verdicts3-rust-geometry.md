1: **CONFIRMED** — Extreme aspect ratios where a dimension scales below 0.5px (e.g. $1 \times 2000$ image with 640 input size) round `inner_w` to 0 and `net_w` to 0, constructing a 0-width tensor $[1, 3, 640, 0]$ passed to ONNX Runtime and resulting in `gain = 0.0` (division by zero) in coordinate inverse mapping ([`geometry.rs:63-80`](file:///Users/caved/dev/manga-typesetter/src-tauri/src/detect/geometry.rs#L63-L80), [`panels.rs:103-125`](file:///Users/caved/dev/manga-typesetter/src-tauri/src/detect/panels.rs#L103-L125), [`geometry.rs:103-108`](file:///Users/caved/dev/manga-typesetter/src-tauri/src/detect/geometry.rs#L103-L108)).

2: **REFUTED** — In both [`decode_yolo11`](file:///Users/caved/dev/manga-typesetter/src-tauri/src/detect/geometry.rs#L197-L208) and [`decode_blocks`](file:///Users/caved/dev/manga-typesetter/src-tauri/src/detect/textdetector.rs#L214-L224), `best_score` is initialized to `f32::MIN`; comparison `s > best_score` evaluates to `false` for `s = NaN`, so `best_score` remains `f32::MIN <= conf` and the candidate is discarded, meaning a `BBox` with `score = NaN` is unreachable in [`nms`](file:///Users/caved/dev/manga-typesetter/src-tauri/src/detect/geometry.rs#L157-L174) ([`geometry.rs:162`](file:///Users/caved/dev/manga-typesetter/src-tauri/src/detect/geometry.rs#L162), [`geometry.rs:197-208`](file:///Users/caved/dev/manga-typesetter/src-tauri/src/detect/geometry.rs#L197-L208), [`textdetector.rs:214-224`](file:///Users/caved/dev/manga-typesetter/src-tauri/src/detect/textdetector.rs#L214-L224)).

3: **REFUTED** — `best_score` starts as `f32::MIN` ([`geometry.rs:197`](file:///Users/caved/dev/manga-typesetter/src-tauri/src/detect/geometry.rs#L197)); if an anchor class score is `f32::NAN`, `s > best_score` evaluates to `false` in Rust float semantics, preventing `best_score` from ever becoming `NaN` and leaving `best_score == f32::MIN <= conf` as `true` at line 208 to execute `continue` ([`geometry.rs:197,200,208`](file:///Users/caved/dev/manga-typesetter/src-tauri/src/detect/geometry.rs#L197-L208)).

4: **CONFIRMED** — An ONNX detection output with valid class score (`> conf`) but `NaN` coordinate values (e.g. `cx = NaN` or `gain == 0.0`), causes `((b.x1 - pad_x) / gain).clamp(0.0, src_w as f32)` to return `NaN` (Rust's `f32::clamp` returns `NaN` for `NaN` input), which downstream in [`detect_panels`](file:///Users/caved/dev/manga-typesetter/src-tauri/src/detect/analyze.rs#L78-L79) casts `NaN as i32` to `0`, generating a false `[0, 0, 0, 0]` panel bbox ([`geometry.rs:108-111`](file:///Users/caved/dev/manga-typesetter/src-tauri/src/detect/geometry.rs#L108-L111), [`analyze.rs:78-79`](file:///Users/caved/dev/manga-typesetter/src-tauri/src/detect/analyze.rs#L78-L79)).

---

### Priority Summary
1. **High Priority (Finding 1)**: Clamp `inner_w` and `inner_h` to at least `1` in [`Letterbox::fit`](file:///Users/caved/dev/manga-typesetter/src-tauri/src/detect/geometry.rs#L63-L65) to prevent 0-dimension network tensors and division by zero on extreme aspect-ratio inputs (e.g. webtoon slices).
2. **Low Priority (Finding 4)**: Ensure coordinates in [`Letterbox::to_source`](file:///Users/caved/dev/manga-typesetter/src-tauri/src/detect/geometry.rs#L108-L111) are checked for finiteness before or after clamping to avoid fabricating `[0, 0, 0, 0]` panel boxes from anomalous model outputs.
3. **No Action Required (Findings 2 & 3)**: Disproved as false positives due to existing `f32::MIN` initializations and IEEE-754 float comparison semantics filtering out NaN scores before candidate generation and sorting.

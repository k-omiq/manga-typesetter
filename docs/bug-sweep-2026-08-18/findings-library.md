1. [`src/lib/library.svelte.js:1114-1144`](file:///Users/caved/dev/manga-typesetter/src/lib/library.svelte.js#L1114-L1144) — HIGH — Chapter switch race window allows autosave/flush to overwrite the previous chapter's disk file with the new chapter's pages — Concrete sequence: (1) Chapter A is open (`app.chapterRef = { projectId: 'p1', chapterId: 'cA' }`). (2) [`openChapter('p1', 'cB')`](file:///Users/caved/dev/manga-typesetter/src/lib/library.svelte.js#L1049) is called; it flushes chapter A, reads chapter B from disk, and at line 1114 executes [`loadProjectPages(pages)`](file:///Users/caved/dev/manga-typesetter/src/lib/store.svelte.js#L110) which replaces `app.pages` with Chapter B's pages and triggers [`markUnsaved()`](file:///Users/caved/dev/manga-typesetter/src/lib/store.svelte.js#L760). (3) At line 1123, [`setResidentWindow`](file:///Users/caved/dev/manga-typesetter/src/lib/page-images.js#L189) is awaited while `app.chapterRef` is STILL Chapter A. (4) If [`flushSave()`](file:///Users/caved/dev/manga-typesetter/src/lib/store.svelte.js#L779) runs during this I/O window (or `saveT` fires), [`writeOpenChapter`](file:///Users/caved/dev/manga-typesetter/src/lib/library.svelte.js#L1187) serializes `app.pages` (Chapter B) and writes it into Chapter A's `chapter.json`, permanently destroying Chapter A's content. — Suggested fix: Null `app.chapterRef` before calling `loadProjectPages` (or immediately after `await flushSave()`), only assigning the new ref once loading and image windowing fully resolve.

2. [`src/lib/library.svelte.js:1049-1160`](file:///Users/caved/dev/manga-typesetter/src/lib/library.svelte.js#L1049-L1160) — HIGH — Lack of sequence token / cancellation in [`openChapter`](file:///Users/caved/dev/manga-typesetter/src/lib/library.svelte.js#L1049) allows concurrent chapter opens to resolve out-of-order and corrupt editor state — Concrete sequence: (1) User opens Chapter 1, which has unmeasured pages and begins awaiting image decodes in `measureIfUnmeasured`. (2) User quickly navigates and opens Chapter 2, which has pre-measured pages and resolves quickly, setting `app.chapterRef = C2` and rendering Chapter 2. (3) Chapter 1's decode loop finishes later and proceeds to call `loadProjectPages(C1)` and `app.chapterRef = C1` while the route/UI is on Chapter 2. (4) Subsequent user edits and autosaves write Chapter 1 pages into Chapter 2's `chapter.json`. — Suggested fix: Introduce an incrementing `openSeq` ticket at the start of `openChapter` and abort state mutations if superseded after any await.

3. [`src/lib/library.svelte.js:1114-1144`](file:///Users/caved/dev/manga-typesetter/src/lib/library.svelte.js#L1114-L1144) — HIGH — Mid-load failure in [`openChapter`](file:///Users/caved/dev/manga-typesetter/src/lib/library.svelte.js#L1049) leaves stale `app.chapterRef`, causing exit navigation to save loaded pages into the wrong chapter — Concrete sequence: (1) Chapter A is open. (2) [`openChapter('p1', 'cB')`](file:///Users/caved/dev/manga-typesetter/src/lib/library.svelte.js#L1049) runs `loadProjectPages(pages)` (mutating `app.pages` to Chapter B), but [`setResidentWindow`](file:///Users/caved/dev/manga-typesetter/src/lib/page-images.js#L189) throws an error before line 1144 (`app.chapterRef = cB`) is reached. (3) `App.svelte` catches the rejection and calls `goBack()`. (4) `route.svelte.js` triggers `leaveEditorHook` -> [`flushBeforeLeaving('editor')`](file:///Users/caved/dev/manga-typesetter/src/lib/library.svelte.js#L1362) -> [`flushSave()`](file:///Users/caved/dev/manga-typesetter/src/lib/store.svelte.js#L779). (5) `flushSave()` sees `app.chapterRef` still pointing to Chapter A and writes Chapter B's `app.pages` into Chapter A's `chapter.json`. — Suggested fix: Clear `app.chapterRef = null` prior to mutating `app.pages` in `openChapter` and wrap the open sequence in a try/catch that resets store state on error.

4. [`src/lib/library.svelte.js:848-897`](file:///Users/caved/dev/manga-typesetter/src/lib/library.svelte.js#L848-L897) & [`900-930`](file:///Users/caved/dev/manga-typesetter/src/lib/library.svelte.js#L900-L930) — HIGH — [`replaceCleanedPages`](file:///Users/caved/dev/manga-typesetter/src/lib/library.svelte.js#L848) and [`setPageCleaned`](file:///Users/caved/dev/manga-typesetter/src/lib/library.svelte.js#L900) omit image dimension measurements, permanently distorting page coordinate space — Concrete sequence: (1) A chapter has raw images measured at 1080x1920 (`pg.w=1080, pg.h=1920`). (2) Cleaned pages at 2160x3840 are imported via `replaceCleanedPages` or `setPageCleaned`; neither function calls `imageSize(bytes)` or updates `pg.w`/`pg.h`. (3) When the chapter is opened, `openChapter` checks `hasPageSpace(pg)` (`w > 0 && h > 0`), treats the page as already measured, and retains 1080x1920. (4) Canvas rendering, box placement, and PSD exports render against mismatched dimensions, clipping or misaligning typesetting boxes over the cleaned art. — Suggested fix: Measure image dimensions via `await imageSize(bytes)` in `replaceCleanedPages` and `setPageCleaned` and update `pg.w`/`pg.h` in `next`.

5. [`src/lib/library.svelte.js:376-382`](file:///Users/caved/dev/manga-typesetter/src/lib/library.svelte.js#L376-L382) — MED — [`deleteChapter`](file:///Users/caved/dev/manga-typesetter/src/lib/library.svelte.js#L376) does not update `project.json` or `p.coverChapterId`, permanently blocking future thumbnail generation — Concrete sequence: (1) Chapter 1 is created as the first chapter in Project P (`p.coverChapterId = 'c1'`). (2) Chapter 1 is deleted via `deleteChapter(p.id, 'c1')`, which deletes the folder on disk but does not write `project.json` or reset `p.coverChapterId`. (3) A new Chapter 2 is imported; [`buildChapter`](file:///Users/caved/dev/manga-typesetter/src/lib/library.svelte.js#L521) evaluates `const willHaveCover = !p.coverChapterId` as `false`. (4) `cover.offer()` never writes `thumb.png`, leaving the project permanently without a thumbnail and referencing a dangling cover chapter ID. — Suggested fix: In `deleteChapter`, if `p.coverChapterId === chapterId`, reassign or null `coverChapterId`/`coverPageId` and write the updated `project.json`.

6. [`src/lib/library.svelte.js:537-545`](file:///Users/caved/dev/manga-typesetter/src/lib/library.svelte.js#L537-L545) & [`613-619`](file:///Users/caved/dev/manga-typesetter/src/lib/library.svelte.js#L613-L619) — MED — Chapter creation failure rollback removes project-level `thumb.png` belonging to other chapters — Concrete sequence: (1) Project P has no cover yet. (2) Two chapter imports run concurrently or in close overlap; both evaluate `willHaveCover = true`. (3) Chapter 1 writes `thumb.png` successfully. (4) Chapter 2 also writes `thumb.png` (`thumbWritten = true`), but fails during page copy. (5) Chapter 2's `catch` block executes `await fsx.remove(await fsx.join(p.dir, 'thumb.png'))`. (6) Chapter 1 succeeds and sets `p.coverChapterId = 'c1'`, but the project thumbnail file was deleted by Chapter 2's rollback and is not recreated. — Suggested fix: Write thumbnails to a temporary file and atomically rename only upon successful chapter creation, or check `p.coverChapterId` before unlinking `thumb.png` on rollback.

7. [`src/lib/library.svelte.js:67-72`](file:///Users/caved/dev/manga-typesetter/src/lib/library.svelte.js#L67-L72) — MED — [`withinHome`](file:///Users/caved/dev/manga-typesetter/src/lib/library.svelte.js#L67) hardcodes POSIX slash separator `/`, breaking library folder selection on Windows — Concrete sequence: (1) On Windows, `fsx.homeDir()` returns `C:\Users\username`. (2) User selects `C:\Users\username\Documents\Manga` in Settings. (3) `withinHome` checks `d.startsWith(home + '/')` (`"C:\\Users\\username/"`), which fails against backslash paths. (4) `SettingsModal.svelte` rejects the valid path with `"The library has to live inside your home folder for now"`, preventing Windows users from selecting directories. — Suggested fix: Normalize path separators to `/` on both `home` and `dir` before testing prefix containment.

8. [`src/lib/library.svelte.js:1195-1250`](file:///Users/caved/dev/manga-typesetter/src/lib/library.svelte.js#L1195-L1250) — LOW — [`writeOpenChapter`](file:///Users/caved/dev/manga-typesetter/src/lib/library.svelte.js#L1187) mutates a disconnected chapter object if [`scanLibrary`](file:///Users/caved/dev/manga-typesetter/src/lib/library.svelte.js#L221) completes concurrently — Concrete sequence: (1) Chapter C is open in editor. (2) `writeOpenChapter` captures `const c = chapterById(...)` and awaits `fsx.readJson` / `fsx.writeJson`. (3) While awaiting, `scanLibrary()` completes and replaces `library.projects` with fresh objects. (4) `writeOpenChapter` resumes and sets `c.updatedAt = record.updatedAt`, mutating the orphaned `c` reference. (5) The active chapter in `library.projects` retains stale `updatedAt` / `pageCount` in the UI catalogue until the next scan. — Suggested fix: Look up the live chapter entry from `library.projects` after disk writes succeed when applying metadata updates.

9. [`src/lib/library.svelte.js:723-744`](file:///Users/caved/dev/manga-typesetter/src/lib/library.svelte.js#L723-L744) — LOW — [`createChapterFromPages`](file:///Users/caved/dev/manga-typesetter/src/lib/library.svelte.js#L723) lacks validation for `rawName`/`cleanedName`, crashing with an unhandled TypeError — Concrete sequence: (1) Caller passes page data where `rawBytes` exists but `rawName` is `undefined` or `null`. (2) `createChapterFromPages` validates `!pg.rawBytes`, but not `pg.rawName`. (3) `copyInto` passes `src.rawName` into `freeFileName` -> [`uniqueFileName`](file:///Users/caved/dev/manga-typesetter/src/lib/library.svelte.js#L396), which executes `name.lastIndexOf('.')`. (4) JavaScript throws `TypeError: Cannot read properties of undefined (reading 'lastIndexOf')`, failing the import unexpectedly. — Suggested fix: Validate that every input page has a non-empty string for `rawName` (and `cleanedName` if `cleanedBytes` is present) before starting `buildChapter`.

10. [`src/lib/store.svelte.js:776`](file:///Users/caved/dev/manga-typesetter/src/lib/store.svelte.js#L776) (interacting with [`src/lib/library.svelte.js:1151`](file:///Users/caved/dev/manga-typesetter/src/lib/library.svelte.js#L1151)) — LOW — [`markSaved`](file:///Users/caved/dev/manga-typesetter/src/lib/store.svelte.js#L776) does not cancel the `saveT` debounce timer, causing ghost autosaves on clean loads — Concrete sequence: (1) In `openChapter`, line 1114 `loadProjectPages` invokes `markUnsaved()`, scheduling `saveT = setTimeout(..., 800)`. (2) At line 1151, `openChapter` calls `markSaved()`, which only sets `app.saved = true` without clearing `saveT`. (3) 800ms later, `saveT` fires `runSavers()` -> `writeOpenChapter`, forcing an unnecessary disk rewrite of an unmodified `chapter.json` and stamping a new `updatedAt`. — Suggested fix: Add `clearTimeout(saveT)` inside `markSaved()`.

---
*Clean areas:* Atomic file writes via [`fsx.writeTextFileAtomic`](file:///Users/caved/dev/manga-typesetter/src/lib/fsx.js#L44) properly use unique per-write sequence numbers and guarantee failure cleanup; duplicate chapter/project ID detection in [`scanLibrary`](file:///Users/caved/dev/manga-typesetter/src/lib/library.svelte.js#L221) cleanly handles copied folder collisions without crashing.

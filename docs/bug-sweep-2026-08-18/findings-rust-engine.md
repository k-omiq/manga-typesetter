[`src-tauri/src/detect/engine.rs:175`](file:///Users/caved/dev/manga-typesetter/src-tauri/src/detect/engine.rs#L175) — HIGH — Concurrent downloads race on the exact same `.part` path without synchronization — If two `detect_analyze` commands run simultaneously on initial run or missing weights, both create and write to `<model>.onnx.part` concurrently, and whichever finishes first renames a corrupted/interleaved weight file to `<model>.onnx`, bricking all future detection calls until a cache clear — One-line fix: Write to a unique temporary filename (e.g. via `tempfile::Builder` or appending a UUID/PID) before atomically renaming to `path`.

[`src-tauri/src/detect/engine.rs:378`](file:///Users/caved/dev/manga-typesetter/src-tauri/src/detect/engine.rs#L378) — MEDIUM — Poisoned mutex is silently ignored in `detect_models_cache_clear` and permanently bricks the engine — If `analyze_blocking` panics, `engine.0.sessions` becomes poisoned; `if let Ok(mut s)` silently fails without resetting the session or clearing the poison, leaving subsequent `detect_analyze` calls permanently failing with `"engine mutex poisoned"` — One-line fix: Recover from a poisoned mutex using `let mut s = engine.0.sessions.lock().unwrap_or_else(|e| e.into_inner());` in both `detect_models_cache_clear` and `analyze_blocking`.

[`src-tauri/src/detect/engine.rs:380`](file:///Users/caved/dev/manga-typesetter/src-tauri/src/detect/engine.rs#L380) — MEDIUM — `detect_models_cache_clear` drops the session lock before deleting the directory — `detect_models_cache_clear` releases the `sessions` mutex before spawning `remove_dir_all`, allowing a concurrent `detect_analyze` that already passed `ensure()` to acquire the free lock and attempt loading ONNX models while `cache_clear` is actively deleting them — One-line fix: Hold the `sessions` lock (or an engine-level lock) across the `cache_clear` blocking task execution.

[`src-tauri/src/detect/engine.rs:190`](file:///Users/caved/dev/manga-typesetter/src-tauri/src/detect/engine.rs#L190) — MEDIUM — Synchronous blocking filesystem I/O and disk syncing directly inside async `download` — `download` executes synchronous `std::fs::File::create`, `write_all`, and `sync_all` on hundreds of megabytes directly within Tokio's async worker thread, stalling the async executor and starving other concurrent async Tauri tasks — One-line fix: Use `tokio::fs` for async streaming file I/O or offload the download write loop to `tokio::task::spawn_blocking`.

[`src-tauri/src/detect/engine.rs:209`](file:///Users/caved/dev/manga-typesetter/src-tauri/src/detect/engine.rs#L209) — LOW — Failed `std::fs::rename` in `download` bypasses error cleanup and leaks `.part` files — In `download`, `remove_file(&tmp)` is only executed in the `Err` branch of the stream future (`result`); if `result` completes but `std::fs::rename(&tmp, path)` fails, the `.part` file is leaked on disk as uncollected litter — One-line fix: Clean up `tmp` with `let _ = std::fs::remove_file(&tmp);` whenever `std::fs::rename(&tmp, path)` returns an `Err`.

[`src-tauri/src/detect/engine.rs:288`](file:///Users/caved/dev/manga-typesetter/src-tauri/src/detect/engine.rs#L288) — LOW — `guard_models_dir` rejects valid cache clear when `models_dir` is a symlink to an external location — `dir.canonicalize()` resolves symlink targets, so if a user symlinks `~/.mangatypesetter/models` to an external directory (e.g. `/Volumes/Data/models`), the canonicalized tail does not match `[".mangatypesetter", "models"]` and `cache_clear` aborts with a false safety error — One-line fix: Validate the path components before resolving symlinks, or canonicalize only the parent directory.

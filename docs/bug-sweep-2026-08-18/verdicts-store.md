1: CONFIRMED — Activating a free-typed box line (`activeLineN = -1`) and calling [`deleteBox`](file:///Users/caved/dev/manga-typesetter/src/lib/store.svelte.js#L1830) removes the line from `p.lines` but leaves `p.activeLineN` dangling at `-1`, causing subsequent canvas clicks in Place mode to fail silently at [`Canvas.svelte:271`](file:///Users/caved/dev/manga-typesetter/src/lib/editor/Canvas.svelte#L271) ([src/lib/store.svelte.js:1851](file:///Users/caved/dev/manga-typesetter/src/lib/store.svelte.js#L1851)).

2: CONFIRMED — Opening inline edit on a queue-placed box (`b.text === null`, `lineN > 0`) initializes `editBefore = null`; blurring or pressing Escape without edits passes the line text into [`endEdit`](file:///Users/caved/dev/manga-typesetter/src/lib/store.svelte.js#L1073), which calls [`setBoxText`](file:///Users/caved/dev/manga-typesetter/src/lib/store.svelte.js#L413) to write an override `b.text = commitText` and records a bogus undo entry, permanently detaching the box from queue updates ([src/lib/store.svelte.js:1087](file:///Users/caved/dev/manga-typesetter/src/lib/store.svelte.js#L1087)).

3: CONFIRMED — Running single-page or batch detection assigns `p.lines = result.lines.map(...)` with `en: ''`, entirely dropping free-typed lines (`n < 0`) and wiping out all translated English text entered in Translate mode ([src/lib/store.svelte.js:1353](file:///Users/caved/dev/manga-typesetter/src/lib/store.svelte.js#L1353)).

4: CONFIRMED — [`applyDetection`](file:///Users/caved/dev/manga-typesetter/src/lib/store.svelte.js#L1337) unconditionally sets `p.boxes = []` without calling [`deselect`](file:///Users/caved/dev/manga-typesetter/src/lib/store.svelte.js#L1043) or [`clearPending`](file:///Users/caved/dev/manga-typesetter/src/lib/store.svelte.js#L885), leaving `app.selectedId` and `app.editingId` pointing to nonexistent box IDs and wiping boxes with no undo history record ([src/lib/store.svelte.js:1368](file:///Users/caved/dev/manga-typesetter/src/lib/store.svelte.js#L1368)).

5: CONFIRMED — In longstrip mode, [`applyBulk`](file:///Users/caved/dev/manga-typesetter/src/lib/store.svelte.js#L2044) searches only `p.boxes` on the active `p = page()`, causing targets selected on other visible page slices to resolve to `undefined` and be silently discarded by `filter(Boolean)` ([src/lib/store.svelte.js:2057](file:///Users/caved/dev/manga-typesetter/src/lib/store.svelte.js#L2057)).

6: CONFIRMED — When deleting a box currently in inline edit mode (e.g. via Inspector's delete button), [`deleteBox`](file:///Users/caved/dev/manga-typesetter/src/lib/store.svelte.js#L1830) nulls `app.selectedId` but omits clearing `app.editingId`, causing [`App.svelte:278`](file:///Users/caved/dev/manga-typesetter/src/App.svelte#L278) to permanently suppress all global keyboard shortcuts ([src/lib/store.svelte.js:1870](file:///Users/caved/dev/manga-typesetter/src/lib/store.svelte.js#L1870)).

7: CONFIRMED — [`markSaved`](file:///Users/caved/dev/manga-typesetter/src/lib/store.svelte.js#L776) sets `app.saved = true` without calling `clearTimeout(saveT)`, allowing any active debounce timer scheduled by [`markUnsaved`](file:///Users/caved/dev/manga-typesetter/src/lib/store.svelte.js#L760) to fire a redundant `runSavers()` disk write 800ms later ([src/lib/store.svelte.js:776](file:///Users/caved/dev/manga-typesetter/src/lib/store.svelte.js#L776)).

8: CONFIRMED — Calling [`rawZoomOut`](file:///Users/caved/dev/manga-typesetter/src/lib/store.svelte.js#L2277) while in Fit mode (`app.rawZoom === 0`) evaluates `(0 === 0 ? 1 : 0) * (1 / 1.25) = 0.8`, setting `app.rawZoom = 0.8` (80% column width) instead of remaining at 0 (Fit) ([src/lib/store.svelte.js:2271](file:///Users/caved/dev/manga-typesetter/src/lib/store.svelte.js#L2271)).

### Priority Summary
- **P0 / High (Data Loss & Schema Violations)**: Findings 2, 3, and 4 cause irreversible translation loss during re-detection, phantom queue detachment overrides on untouched blur, and destroyed boxes with uncleaned selection pointers.
- **P1 / Medium (Broken State & UI Interaction Flow)**: Findings 1, 5, 6, and 7 break subsequent Place mode canvas clicks on free-box deletion, drop cross-page bulk targets in longstrip mode, lock global shortcuts on inline deletion, and execute uncancelled disk writes.
- **P2 / Low (Visual Polish)**: Finding 8 shrinks the sidebar reference zoom below Fit to 0.8x on initial zoom out instead of staying pinned to Fit mode.

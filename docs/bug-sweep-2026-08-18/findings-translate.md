src/lib/library.svelte.js:809 — HIGH — `commitPages` does not write `translations.json`, causing `translations.json` to permanently diverge from `chapter.json` upon source mutations — When `applyTranslations` imports a translation file onto a closed chapter, `chapter.json` receives the new lines but `translations.json` is never rewritten on disk and remains permanently stale — Add `await writeTranslationsJson(c.dir, buildTextJson(pages));` inside `commitPages`.

src/lib/library.svelte.js:1060 — HIGH — `openChapter` retains `app.chapterRef` from the previous chapter during `loadProjectPages`, arming an unintended autosave against the wrong chapter — Opening chapter B while chapter A is open triggers `markUnsaved()` with chapter A's ref; if image decoding or `setResidentWindow` takes >800ms, autosave writes chapter B's pages into chapter A's `chapter.json` — Set `app.chapterRef = null;` immediately after `await flushSave();` on line 1060.

src/lib/library.svelte.js:1123 — MEDIUM — `app.chapterMode` and `app.projectLayout` are updated after `setResidentWindow`, leaking the previous chapter's mode and layout during page loading — Opening a typeset paged chapter B after a translate longstrip chapter A causes `residentRadiusFor` and reactive editor components to run against chapter A's mode and layout while awaiting image decoding — Move `app.chapterMode = normalizeChapterMode(record.mode);` and `app.projectLayout = normalizeLayout(p.layout);` before `loadProjectPages`.

src/lib/store.svelte.js:2219 — HIGH — `activateLine` selects canvas text boxes in translate mode, allowing box and line deletion via the Delete/Backspace shortcut — In a translate-mode chapter with existing typeset boxes, clicking a queue row calls `selectBox(b.id)`; pressing Backspace or Delete triggers `deleteBox`, destroying canvas boxes and free lines in a read-only translation workspace — In `activateLine`, guard selection with `if (b && !isTranslateMode()) selectBox(b.id); else deselect();`.

src/lib/editor/history.svelte.js:335 — MEDIUM — `undo` and `redo` do not check `isTranslateMode()`, allowing undo/redo shortcuts to restore or place text boxes in translate mode — When opening a chapter in translate mode that has undo history from earlier typesetting, pressing ⌘Z or ⇧⌘Z replays `place` or `delete` actions, inserting boxes and free lines onto a translate-only canvas — Add `if (isTranslateMode()) return false;` at the start of `undo()` and `redo()`.

src/lib/Queue.svelte:66 — MEDIUM — `menuFor` and `form` store un-scoped line numbers, causing tag popovers and forms to leak across page turns — Opening a tag form on line 1 of page 1 and navigating to page 2 leaves the form open on page 2's line 1; submitting the form applies tag defaults to page 2's line instead — Add `$effect(() => { p.id; menuFor = null; form = null; });` in `Queue.svelte`.

src/lib/text-json.js:28 — LOW — Nullish coalescing `0 ?? PAGE_W` writes zero width/height for unmeasured pages in JSON exports — Exporting text JSON on a chapter containing unmeasured pages (`w: 0, h: 0`) outputs `"width": 0, "height": 0` instead of falling back to default page dimensions — Replace `p.w ?? PAGE_W` with `p.w > 0 ? p.w : PAGE_W` and `p.h ?? PAGE_H` with `p.h > 0 ? p.h : PAGE_H`.

src/lib/editor/ChromePills.svelte:31 — LOW — `saveJson` does not settle inline edits before exporting text JSON — Clicking the JSON button in translate mode while an active text edit is open exports pre-edit content because `settleEdits()` was not called before `buildTextJson` — Call `settleEdits();` before `exportTextJson('all')` in `saveJson()`.

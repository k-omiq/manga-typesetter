1. [src-tauri/src/detect/ocr.rs:569-573](file:///Users/caved/dev/manga-typesetter/src-tauri/src/detect/ocr.rs#L569-L573) — **SEVERITY(med)** — `log_softmax` is applied to logits before masking banned n-gram tokens rather than after — When a beam encounters a banned token that carried substantial probability mass $P$ (e.g. 50%), setting it to $-\infty$ after `log_softmax` leaves the beam's unbanned candidates un-renormalized (their probabilities sum to $1 - P < 1$), artificially deflating all continuation scores of that beam by $\ln(1 - P)$ and allowing competing beams without bans to incorrectly overtake it — Mask `row[*t as usize] = f32::NEG_INFINITY` before calling `log_softmax(row)` instead of after.

2. [src-tauri/src/detect/ocr.rs:242-264](file:///Users/caved/dev/manga-typesetter/src-tauri/src/detect/ocr.rs#L242-L264) — **SEVERITY(low)** — `HALF_TO_FULL_KANA` table omits standalone half-width dakuten (`\u{FF9E}`) and handakuten (`\u{FF9F}`) mappings from `jaconv`'s `HALF_KANA_SEION` — OCR output containing standalone or non-katakana voiced sound marks (e.g. `あﾞ` / `\u{FF9E}`) leaves raw half-width marks intact rather than converting them to full-width `゛` (`\u{309B}`) / `゜` (`\u{309C}`) as `jaconv.h2z` does in Python — Append `('\u{FF9E}', '\u{309B}')` and `('\u{FF9F}', '\u{309C}')` to [`HALF_TO_FULL_KANA`](file:///Users/caved/dev/manga-typesetter/src-tauri/src/detect/ocr.rs#L242-L264).

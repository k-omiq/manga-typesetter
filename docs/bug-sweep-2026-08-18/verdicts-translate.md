1: CONFIRMED — Minimal trigger: Calling [`applyTranslations`](file:///Users/caved/dev/manga-typesetter/src/lib/library.svelte.js#L969) (or page operations like `replaceCleanedPages` / `reorderPages`) on a closed chapter updates `chapter.json` via [`commitPages`](file:///Users/caved/dev/manga-typesetter/src/lib/library.svelte.js#L809), but [`writeTranslationsJson`](file:///Users/caved/dev/manga-typesetter/src/lib/library.svelte.js#L117) is never invoked, leaving `translations.json` on disk permanently stale until the chapter is subsequently opened and saved. ([library.svelte.js:809](file:///Users/caved/dev/manga-typesetter/src/lib/library.svelte.js#L809))

2: CONFIRMED — Minimal trigger: Opening chapter B while chapter A is open retains Chapter A's ref in `app.chapterRef` after [`flushSave()`](file:///Users/caved/dev/manga-typesetter/src/lib/library.svelte.js#L1060). When [`loadProjectPages`](file:///Users/caved/dev/manga-typesetter/src/lib/library.svelte.js#L1114) replaces `app.pages` with Chapter B's data and calls [`markUnsaved()`](file:///Users/caved/dev/manga-typesetter/src/lib/store.svelte.js#L213), an 800ms autosave timer is armed against Chapter A's ref; if subsequent async work in [`setResidentWindow`](file:///Users/caved/dev/manga-typesetter/src/lib/library.svelte.js#L1123) takes >800ms, Chapter B's pages are written into Chapter A's `chapter.json`. ([library.svelte.js:1060](file:///Users/caved/dev/manga-typesetter/src/lib/library.svelte.js#L1060))

3: CONFIRMED — Minimal trigger: Opening chapter B (with different mode or layout) while chapter A is open replaces `app.pages` synchronously, immediately firing reactive effects like [`EditorRoot.svelte:$effect`](file:///Users/caved/dev/manga-typesetter/src/lib/editor/EditorRoot.svelte#L98) and derived UI state while [`app.chapterMode` and `app.projectLayout`](file:///Users/caved/dev/manga-typesetter/src/lib/library.svelte.js#L1130-L1136) still reflect chapter A during the `await setResidentWindow` async window. ([library.svelte.js:1123](file:///Users/caved/dev/manga-typesetter/src/lib/library.svelte.js#L1123))

4: CONFIRMED — Minimal trigger: In translate mode on a chapter containing existing placed or free text boxes, clicking a line row in the queue triggers [`activateLine`](file:///Users/caved/dev/manga-typesetter/src/lib/store.svelte.js#L2219) which selects the canvas box with `selectBox(b.id)`. Pressing Delete or Backspace outside an input triggers [`deleteBox(app.selectedId)`](file:///Users/caved/dev/manga-typesetter/src/App.svelte#L319), mutating `p.boxes` and deleting free lines in a translation workspace. ([store.svelte.js:2219](file:///Users/caved/dev/manga-typesetter/src/lib/store.svelte.js#L2219))

5: CONFIRMED — Minimal trigger: In translate mode on a chapter with undo/redo history persisted on disk from prior typesetting, pressing ⌘Z or ⇧⌘Z executes [`undo()` or `redo()`](file:///Users/caved/dev/manga-typesetter/src/lib/editor/history.svelte.js#L335-L342) without checking `isTranslateMode()`, replaying box placement, deletion, and movement onto the canvas and marking the chapter unsaved. ([history.svelte.js:335](file:///Users/caved/dev/manga-typesetter/src/lib/editor/history.svelte.js#L335))

6: CONFIRMED — Minimal trigger: Opening a tag creation/edit form or tag menu on line $N$ of page $X$, then navigating to page $Y$ (where line $N$ exists), leaves [`form` and `menuFor`](file:///Users/caved/dev/manga-typesetter/src/lib/Queue.svelte#L66-L72) set to $N$ because component state is un-scoped to page ID; submitting the form saves or applies defaults to page $Y$'s line $N$. ([Queue.svelte:66](file:///Users/caved/dev/manga-typesetter/src/lib/Queue.svelte#L66))

7: CONFIRMED — Minimal trigger: Exporting text JSON on a chapter with unmeasured pages (`w: 0, h: 0`) causes [`width: p.w ?? PAGE_W`](file:///Users/caved/dev/manga-typesetter/src/lib/text-json.js#L28) and `height: p.h ?? PAGE_H` to evaluate `0 ?? PAGE_W` as `0`, outputting `"width": 0, "height": 0` instead of falling back to default dimensions `PAGE_W` / `PAGE_H`. ([text-json.js:28](file:///Users/caved/dev/manga-typesetter/src/lib/text-json.js#L28))

8: REFUTED — Guard: [`onEditInput`](file:///Users/caved/dev/manga-typesetter/src/lib/TextBox.svelte#L411-L416) calls `setBoxText(box, v)` and [`Queue.svelte:390`](file:///Users/caved/dev/manga-typesetter/src/lib/Queue.svelte#L390) assigns `line.en = e.currentTarget.value` synchronously on every keystroke, updating store data immediately before [`buildTextJson`](file:///Users/caved/dev/manga-typesetter/src/lib/text-json.js#L20-L57) reads it. [`settleEdits()`](file:///Users/caved/dev/manga-typesetter/src/lib/store.svelte.js#L828-L852) only commits records into the undo history stack and does not flush text from DOM to store. ([ChromePills.svelte:31](file:///Users/caved/dev/manga-typesetter/src/lib/editor/ChromePills.svelte#L31))

### Priority Summary
- **P0 / High**: Findings 2 and 1 risk data corruption and permanent divergence between `chapter.json` and `translations.json`.
- **P1 / Medium**: Findings 4 and 5 violate translate-mode immutability by allowing box deletions and history replays, while Finding 3 leaks layout/mode state during page loading and Finding 6 leaks active forms across page turns.
- **P2 / Low**: Finding 7 causes export zero-dimension bugs on unmeasured pages; Finding 8 is refuted as a false positive.

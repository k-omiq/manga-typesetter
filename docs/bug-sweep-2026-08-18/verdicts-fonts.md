1: CONFIRMED — `normalizeTranslations(null)` throws `TypeError: Cannot read properties of null (reading 'pages')` when evaluating `else if (data.pages)`, and `normalizeTranslations({ pages: {} })` throws `TypeError: rawPages.map is not a function` ([src/lib/importer.js:64](file:///Users/caved/dev/manga-typesetter/src/lib/importer.js#L64)).

2: CONFIRMED — `normalizeTranslations([null])` throws `TypeError: Cannot read properties of null (reading 'texts')` at line 62, and `normalizeTranslations({ texts: [null] })` throws `TypeError: Cannot read properties of null (reading 'n')` in `normLine` because `typeof null === 'object'` ([src/lib/importer.js:27](file:///Users/caved/dev/manga-typesetter/src/lib/importer.js#L27)).

3: CONFIRMED — Passing page array `[{ text_lines: [{ text: "Hello" }] }]` fails `(data[0].texts || data[0].lines || data[0].page != null)`, wrapping it into `[{ texts: data }]` and parsing page objects as empty lines, dropping all text ([src/lib/importer.js:62](file:///Users/caved/dev/manga-typesetter/src/lib/importer.js#L62)).

4: CONFIRMED — Passing 0-indexed lines `[{ n: 0, en: "A" }, { n: 1, en: "B" }]` evaluates `Number(0) || 1 -> 1` and `Number(1) || 2 -> 1`, assigning `n: 1` to both lines and making subsequent lookups in [`lineByN`](file:///Users/caved/dev/manga-typesetter/src/lib/store.svelte.js#L272) shadow the second line ([src/lib/importer.js:36](file:///Users/caved/dev/manga-typesetter/src/lib/importer.js#L36)).

5: REFUTED — Nullish coalescing (`??`) is intentional by design across the codebase ([store.svelte.js:367-371](file:///Users/caved/dev/manga-typesetter/src/lib/store.svelte.js#L367-L371), [psd.js:213](file:///Users/caved/dev/manga-typesetter/src/lib/psd.js#L213), [importer.js:14-16](file:///Users/caved/dev/manga-typesetter/src/lib/importer.js#L14-L16)) to fall back only when a field is absent/nullish ("if the line has no `en` field"), preserving explicitly empty strings (`""`) without falling back to alternative keys ([src/lib/importer.js:32](file:///Users/caved/dev/manga-typesetter/src/lib/importer.js#L32)).

6: CONFIRMED — For UTF-16BE name records with odd byte lengths, `TextDecoder('utf-16be').decode()` without `{ fatal: true }` does not throw, returning a string containing replacement character `\uFFFD` (e.g. `"Arial-BoldMT\uFFFD"`) which prevents PostScript font matching in Photoshop ([src/lib/fonts.js:165](file:///Users/caved/dev/manga-typesetter/src/lib/fonts.js#L165)).

7: CONFIRMED — If a user adds or replaces a font face via [`addFontFile`](file:///Users/caved/dev/manga-typesetter/src/lib/fonts.js#L581) while [`restoreFonts`](file:///Users/caved/dev/manga-typesetter/src/lib/fonts.js#L543) is awaiting intermediate `registerFace` calls, `restoreFonts` does not check if the slot is already occupied and overwrites the newly loaded font face with stale IndexedDB bytes ([src/lib/fonts.js:554](file:///Users/caved/dev/manga-typesetter/src/lib/fonts.js#L554)).

### Priority Summary
1. **High Priority**: Fix [importer.js](file:///Users/caved/dev/manga-typesetter/src/lib/importer.js) crashes on `null`/non-array pages (Finding 1), `null` line items (Finding 2), and missing array detection for `text_lines`/`items`/`translations` keys (Finding 3).
2. **Medium Priority**: Fix 0-index line number collision `n: 0` in [importer.js](file:///Users/caved/dev/manga-typesetter/src/lib/importer.js) (Finding 4) and odd-byte UTF-16BE `TextDecoder` `\uFFFD` corruption in [fonts.js](file:///Users/caved/dev/manga-typesetter/src/lib/fonts.js) (Finding 6).
3. **Low / No Action**: Guard against mid-restore font replacement race conditions in [fonts.js](file:///Users/caved/dev/manga-typesetter/src/lib/fonts.js) (Finding 7); reject Finding 5 as intended codebase-wide nullish fallback semantics.

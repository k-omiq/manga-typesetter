src/lib/editor/history.svelte.js:315 — HIGH — `step()` executes undo and redo stack transitions but never notifies `sink`, leaving `history.json` unscheduled for debounced persistence. — A user makes edits and later presses undo/redo; `chapter.json` autosaves the modified document after 800ms, while `history.json` on disk remains in the pre-undo state. If the app crashes, is killed, or loses power before a page switch or chapter close, relaunching loads the post-undo document with the pre-undo history journal, causing replay divergence (applying deltas to states they were not recorded against) on subsequent undo presses. — Call `sink?.(entry.pageId)` inside `step()` after `to.push(entry)` and `sync()`.

src/lib/editor/history-file.svelte.js:180 — HIGH — `openHistory()` async file read executes fire-and-forget; upon resolving, it unconditionally assigns `doc = loaded` and calls `loadStack(pageId, ...)` with the initial `pageId`, wiping in-memory edits and page switches made during disk I/O. — `openChapter()` calls `openHistory()` without awaiting. If the user edits a box or navigates to page 2 (`gotoPage(1)`) while `fsx.readTextFile()` is in flight, `openHistory()` resumes, overwrites `doc` with the disk contents, and loads page 1's stack into memory while the user is on page 2, discarding edits and causing subsequent undo actions to target the wrong page. — Merge `loaded` into current `doc` and load `livePageId` instead of unconditionally assigning `doc = loaded` and loading the initial `pageId`.

src/lib/editor/history-file.svelte.js:283 — HIGH — `closeHistory()` trailing cleanup compares directory path string equality (`dir !== mine`) instead of a session identity token, destroying the newly opened session when reopening the same chapter during an in-flight close flush. — When closing chapter A and immediately reopening chapter A, `openHistory()` sets `dir = pathA` and loads its stack while `closeHistory()`'s `flushHistory()` is still awaiting. When `closeHistory()` finishes flushing, `dir !== mine` evaluates to false (both are `pathA`), so it runs cleanup and sets `dir = null`, `doc = emptyDoc()`, and calls `resetHistory()`, wiping the active undo stack and permanently disabling history saving for the reopened session. — Track an incrementing session ID/token and bail out in `closeHistory()` if `currentSessionToken !== closeSessionToken`.

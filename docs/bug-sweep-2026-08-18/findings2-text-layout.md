I've launched the probe suite and am waiting for the results to complete.
Waiting for task completion.
Waiting for task completion.
Waiting for task completion.
1. [`src/lib/TextBox.svelte:324`](file:///Users/caved/dev/manga-typesetter/src/lib/TextBox.svelte#L324) — **SEVERITY(high)** — Rotation handle dragging clamps angles in the 180°..270° range to 180° instead of wrapping to -180°..-90°, causing cursor rotation in the bottom-left quadrant to freeze at 180° and making negative rotation angles (-90° to -180°) unreachable.
   - *Failure scenario (DEMONSTRATED)*: Dragging the rotation handle to the bottom-left (e.g. 7:30 o'clock position / 225°) evaluates `(atan2 * 180 / Math.PI) + 90 = 225°`; `clamp(225, -180, 180)` snaps `rotation` to 180° rather than wrapping to -135°, and moving across the horizontal left axis jumps abruptly from -89° to 180°.
   - *One-line fix*: `let ang = (Math.atan2(my - cy(), mx - cx()) * 180) / Math.PI + 90; if (ang > 180) ang -= 360; else if (ang < -180) ang += 360; box.style.rotation = clamp(Math.round(ang), -180, 180);`

2. [`src/lib/balloon.js:631`](file:///Users/caved/dev/manga-typesetter/src/lib/balloon.js#L631) — **SEVERITY(med)** — `fitRect` inlier refit reuses unrefined bounding box vertical extents (`y0` and `y1` from `pts`) instead of `ps` (the inliers), retaining outlier rows in `alt.h` and causing rectangular balloons with noisy or panel-border edge rows to drop below `minCoverage` and get rejected as `irregular`.
   - *Failure scenario (DEMONSTRATED)*: A rectangular bubble with 15 body rows and 2 outlier edge rows produces `ev.inliers` of length 15; `build(ev.inliers)` still computes `h = y1 - y0 = 17` from `pts`, yielding `coverage = 15/17 = 0.88` (< `minCoverage: 0.90`) and failing fit detection. With `y0`/`y1` derived from `ps`, coverage is `15/15 = 1.0` and the box is successfully fitted.
   - *One-line fix*: In [`fitRect`](file:///Users/caved/dev/manga-typesetter/src/lib/balloon.js#L628-L633), define `const py0 = ps[0].y - 0.5; const py1 = ps[ps.length - 1].y + 0.5; return { kind: 'rect', x: c - w / 2, y: py0, w, h: py1 - py0 };`.

3. [`src/lib/TextBox.svelte:105`](file:///Users/caved/dev/manga-typesetter/src/lib/TextBox.svelte#L105) — **SEVERITY(med)** — `TextBox.svelte` passes raw un-cased `text` to [`arcLayout`](file:///Users/caved/dev/manga-typesetter/src/lib/measure.js#L52) without applying [`applyCase(text, s)`](file:///Users/caved/dev/manga-typesetter/src/lib/measure.js#L37), causing `arcLayout` to compute character positions and arc radius using lowercase glyph widths while rendering uppercase glyphs via CSS `text-transform: uppercase`.
   - *Failure scenario (DEMONSTRATED)*: For text with `s.uppercase: true`, the editor's [`arcLayout`](file:///Users/caved/dev/manga-typesetter/src/lib/TextBox.svelte#L105) measures lowercase characters (`w`, `m`, etc.), positioning wider uppercase glyphs at tighter lowercase offsets (causing character overlap/misalignment) and diverging from [`exporter.js:131`](file:///Users/caved/dev/manga-typesetter/src/lib/exporter.js#L131) which computes `arcLayout` on `applyCase(text, s)`.
   - *One-line fix*: `const layout = $derived.by(() => s.curve && s.curve !== 0 && !editing && text !== '' ? arcLayout(applyCase(text, s), s, effSize) : null);`

4. [`src/lib/measure.js:55-58`](file:///Users/caved/dev/manga-typesetter/src/lib/measure.js#L55-L58) — **SEVERITY(med)** — [`arcLayout`](file:///Users/caved/dev/manga-typesetter/src/lib/measure.js#L52) produces arrays of `NaN` coordinates when `style.size === 0` or `style.letterSpacing` is `undefined` because `ls` evaluates to `Infinity` or `NaN`, which bypasses `if (total <= 0)` (`NaN <= 0` is false) and propagates `NaN` to `R`, `x`, `y`, and `rot`.
   - *Failure scenario (DEMONSTRATED)*: Calling `arcLayout('hello', { size: 0, letterSpacing: 0, curve: 50 }, 20)` returns objects with `{ ch: 'h', x: NaN, y: NaN, rot: NaN }`, resulting in invalid DOM transforms `translate(calc(-50% + NaNpx), calc(-50% + NaNpx)) rotate(NaNrad)`.
   - *One-line fix*: `const ls = (style.letterSpacing ?? 0) * (style.size > 0 ? sizePx / style.size : 1); if (!(total > 0) || !Number.isFinite(total)) return [];`

5. [`src/lib/measure.js:82, 111, 161`](file:///Users/caved/dev/manga-typesetter/src/lib/measure.js#L82) — **SEVERITY(med)** — [`wrapLines`](file:///Users/caved/dev/manga-typesetter/src/lib/measure.js#L79), [`wrapLinesDOM`](file:///Users/caved/dev/manga-typesetter/src/lib/measure.js#L109), and [`lineWidth`](file:///Users/caved/dev/manga-typesetter/src/lib/measure.js#L158) calculate `ls = style.letterSpacing * (sizePx / style.size)` without fallback defaults for missing `letterSpacing` or zero `style.size`.
   - *Failure scenario (REASONED)*: If `style.letterSpacing` is `undefined` or `style.size === 0`, `ls` is `NaN`. In `wrapLines`, `widthOf(trial)` returns `NaN`, causing `widthOf(trial) > maxWidth` to evaluate to `false` and failing to wrap all words onto new lines. In `lineWidth`, `NaN` width breaks DP cost calculations in [`typeset.js`](file:///Users/caved/dev/manga-typesetter/src/lib/typeset.js).
   - *One-line fix*: Change `const ls = ...` in all three functions to `const ls = (style.letterSpacing ?? 0) * (style.size > 0 ? sizePx / style.size : 1);`.

6. [`src/lib/measure.js:97`](file:///Users/caved/dev/manga-typesetter/src/lib/measure.js#L97) — **SEVERITY(low)** — [`wrapLines`](file:///Users/caved/dev/manga-typesetter/src/lib/measure.js#L79) strips trailing spaces for wrapped lines inside the word loop but pushes the paragraph's final line without trimming trailing whitespace, diverging from [`wrapLinesDOM:147`](file:///Users/caved/dev/manga-typesetter/src/lib/measure.js#L147) and [`typeset.js`](file:///Users/caved/dev/manga-typesetter/src/lib/typeset.js).
   - *Failure scenario (DEMONSTRATED)*: Wrapping `"hello world   "` with [`wrapLines`](file:///Users/caved/dev/manga-typesetter/src/lib/measure.js#L79) yields `["hello world   "]` whereas [`wrapLinesDOM`](file:///Users/caved/dev/manga-typesetter/src/lib/measure.js#L109) outputs `["hello world"]`.
   - *One-line fix*: In [`wrapLines`](file:///Users/caved/dev/manga-typesetter/src/lib/measure.js#L97), change `out.push(line);` to `out.push(line.replace(/\s+$/, ''));`.

1: CONFIRMED — Concurrent [`detect_analyze`](file:///Users/caved/dev/manga-typesetter/src-tauri/src/detect/engine.rs#L342-L357) invocations on a cold cache race on deterministic `<model>.onnx.part` path without synchronization, resulting in file truncation/corruption ([`src-tauri/src/detect/engine.rs:175-190`](file:///Users/caved/dev/manga-typesetter/src-tauri/src/detect/engine.rs#L175-L190)).

2: CONFIRMED — A panic in [`analyze_blocking`](file:///Users/caved/dev/manga-typesetter/src-tauri/src/detect/engine.rs#L126-L160) poisons `engine.0.sessions`; [`detect_models_cache_clear`](file:///Users/caved/dev/manga-typesetter/src-tauri/src/detect/engine.rs#L372-L384)'s `if let Ok` silently ignores `PoisonError` without clearing poison or resetting sessions, permanently bricking subsequent [`detect_analyze`](file:///Users/caved/dev/manga-typesetter/src-tauri/src/detect/engine.rs#L342-L357) calls ([`src-tauri/src/detect/engine.rs:129, 378`](file:///Users/caved/dev/manga-typesetter/src-tauri/src/detect/engine.rs#L129-L378)).

3: CONFIRMED — [`detect_models_cache_clear`](file:///Users/caved/dev/manga-typesetter/src-tauri/src/detect/engine.rs#L372-L384) drops the `sessions` mutex before spawning [`cache_clear`](file:///Users/caved/dev/manga-typesetter/src-tauri/src/detect/engine.rs#L305-L334), allowing a concurrent [`detect_analyze`](file:///Users/caved/dev/manga-typesetter/src-tauri/src/detect/engine.rs#L342-L357) to acquire the lock and reload models while `remove_dir_all` is actively deleting them ([`src-tauri/src/detect/engine.rs:322, 380-381`](file:///Users/caved/dev/manga-typesetter/src-tauri/src/detect/engine.rs#L322-L381)).

4: CONFIRMED — [`download`](file:///Users/caved/dev/manga-typesetter/src-tauri/src/detect/engine.rs#L169-L215) executes synchronous blocking filesystem calls (`std::fs::File::create`, `write_all`, `sync_all`) on hundreds of megabytes directly within Tokio's async worker thread instead of using `spawn_blocking` or async I/O ([`src-tauri/src/detect/engine.rs:190-203`](file:///Users/caved/dev/manga-typesetter/src-tauri/src/detect/engine.rs#L190-L203)).

5: CONFIRMED — When the download stream succeeds but `std::fs::rename` fails, error handling in [`download`](file:///Users/caved/dev/manga-typesetter/src-tauri/src/detect/engine.rs#L169-L215) skips `remove_file(&tmp)` because cleanup is only present in the `Err(e)` stream arm, leaking `.part` files on disk ([`src-tauri/src/detect/engine.rs:209-213`](file:///Users/caved/dev/manga-typesetter/src-tauri/src/detect/engine.rs#L209-L213)).

6: REFUTED — Canonicalizing the directory in [`guard_models_dir`](file:///Users/caved/dev/manga-typesetter/src-tauri/src/detect/engine.rs#L287-L303) before verifying `[".mangatypesetter", "models"]` is an intentional safety guard ensuring `remove_dir_all` cannot wipe arbitrary directories via external symlinks ([`src-tauri/src/detect/engine.rs:288-301`](file:///Users/caved/dev/manga-typesetter/src-tauri/src/detect/engine.rs#L288-L301)).

### Priority Summary
1. **High Priority**: Fix #1 (use unique temporary file paths during download) to prevent corrupted model weights and permanent detection failures on concurrent startup.
2. **Medium Priority**: Fix #2 (recover poisoned mutex on cache clear/analyze), #3 (hold engine coordination lock across `remove_dir_all`), and #4 (offload blocking file writes/syncs off Tokio executor).
3. **Low Priority / Reject**: Fix #5 (cleanup `.part` file on rename failure) as routine hygiene; reject #6 to preserve the intended symlink safety guard.

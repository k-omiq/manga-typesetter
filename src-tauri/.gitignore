# Generated by Cargo
# will have compiled files and executables
/target/
/gen/schemas

# Bundled child binaries. Nothing builds into here any more — the PyInstaller
# sidecar bundle that used to live here went with the Python sidecar — but a
# stale copy may still be on disk, and it must never be committed.
/binaries/
